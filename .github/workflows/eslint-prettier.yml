name: Check the source code
on: [push]
jobs:
  test:
    name: Frontend prettier
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: |
          cd "frontend"
          npm install
      - name: Run Prettier
        run: |
          cd frontend
          npm run format